# BNDB

BNDB is a Python project scaffold for collecting Binance market data, processing it with pandas, and persisting records in SQLite.

## Development

```bash
pip install -r requirements.txt
pip install -e .
pytest
```


# BNDB

BNDB is a Binance Futures abnormal-move research toolkit. It stores 5m market data in SQLite, detects abnormal events, computes pre-event features and 4h outcomes, and renders a compact report with whitelist markers.

## Development

```bash
pip install -r requirements.txt
pip install -e .
pytest
```
